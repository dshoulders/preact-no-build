// Could get this list from the engine when starting the flow
// Or just a get list of the custom components from the engine and merge them with internal list

export default {
    'list-item': '/js/components/ListItem.js',
};